import { Metadata } from "next"

export const metadata: Metadata = {
  title: 'Login Page - Next-14',
  description: 'Login Page for the application'
}

const LoginPage = () => {
  return (
    <h2>Login Page</h2>
  )
}

export default LoginPage